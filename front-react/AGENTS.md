# Instructions

Ce dossier contient le frontend Next.js.

- Utiliser `npm` comme gestionnaire de paquets.
- Lancer le développement avec `npm run dev`.
- Vérifier avant livraison avec `npm run lint` et `npm run build`.
